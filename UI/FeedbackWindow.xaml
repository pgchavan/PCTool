<Window x:Class="HPDTool.UI.FeedbackWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="HPD Tools - Send Feedback"
        Height="350" Width="450"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Your Feedback / Suggestions:"
                   FontWeight="Bold"
                   Margin="0 0 0 8"/>

        <TextBox x:Name="FeedbackBox"
                 Grid.Row="1"
                 AcceptsReturn="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 FontSize="14" />

        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0 12 0 0">

            <Button Content="Send"
                    Width="100" Height="30"
                    Margin="0 0 10 0"
                    Click="Send_Click"/>

            <Button Content="Cancel"
                    Width="100" Height="30"
                    Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>