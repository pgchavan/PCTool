<Window x:Class="HPDTool.UI.LinkModelsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="HPD Tools - Link Revit Models"
        Height="350"
        Width="550"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Placement Option -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Link Placement:" 
                       VerticalAlignment="Center"
                       Margin="0,0,10,0"/>

            <ComboBox x:Name="PlacementCombo"
                      Width="200"
                      SelectedIndex="0">
                <ComboBoxItem Content="By Shared Coordinates"/>
                <ComboBoxItem Content="Origin to Origin"/>
                <ComboBoxItem Content="Center to Center"/>
            </ComboBox>
        </StackPanel>

        <!-- File List -->
        <GroupBox Header="Revit Models" Grid.Row="1">
            <DockPanel>
                <Button Content="Browse..."
                        DockPanel.Dock="Top"
                        Margin="5"
                        Width="100"
                        Click="Browse_Click"/>

                <ListBox x:Name="FilesList"
                         Margin="5"/>
            </DockPanel>
        </GroupBox>

        <!-- Action Buttons -->
        <StackPanel Orientation="Horizontal"
                    Grid.Row="2"
                    HorizontalAlignment="Right"
                    Margin="0,10,0,0">
            <Button Content="OK"
                    Width="80"
                    Margin="5"
                    Click="Ok_Click"/>
            <Button Content="Cancel"
                    Width="80"
                    Margin="5"
                    Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
