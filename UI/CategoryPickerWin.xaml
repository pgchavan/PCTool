<Window x:Class="HPDTool.UI.CategoryPickerWin"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="HPD Tools - Select Category" Height="420" Width="600" WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip" MinHeight="300" MinWidth="320">
    <DockPanel Margin="12">
        <TextBlock DockPanel.Dock="Top" Text="Pick an element category:" FontSize="14" Margin="0,0,0,8"/>
        <ListBox x:Name="CategoryList" SelectionMode="Single" Width="300"/>
        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button Width="90" Margin="0,0,8,0" Click="Ok_Click">OK</Button>
            <Button Width="90" Click="Cancel_Click">Cancel</Button>
        </StackPanel>
    </DockPanel>
</Window>